import React from 'react';
import {View, Text} from 'react-native';

interface Props {}

const Counter = (_props: Props) => {
	return (
		<View>
			<Text>Counting</Text>
		</View>
	);
};

export default Counter;
